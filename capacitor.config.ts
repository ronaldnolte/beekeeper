import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.beektools.beekeeper',
  appName: 'BeekTools Beekeeper',
  webDir: 'dist'
};

export default config;
